# !!! REMOVE .example from the end of the file when using

# Bambulabs Printer Configuration
PRINTER_IP=192.168.0.0
PRINTER_SERIAL=000000000000000
PRINTER_ACCESS_CODE=00000000

# Debug mode (set to True to enable)
DEBUG=False

# Optional. If unset or invalid, the API generates a valid Fernet key on first use.
# Generate manually with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BAMBU_CREDENTIALS_KEY=
